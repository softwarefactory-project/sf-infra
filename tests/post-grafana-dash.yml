---
- hosts: all
  tasks:
    - name: Copy screenshot files
      synchronize:
        src: "{{ ansible_user_dir }}/log/"
        dest: "{{ zuul.executor.log_root }}/log"
        mode: pull
        verify_host: true
