# Code generated by dhall-to-yaml.  DO NOT EDIT.
groups:
  - name: dlrn.rules
    rules:
      - alert: RDOTrunkRepoTooOld
        annotations:
          summary: "No new activity in {{ $labels.worker }} for the last two hours"
        expr: "time() - dlrn_last_update{instance=~'centos8-.*'} > 7200"
        labels:
          lasttime: "{{ $value | humanizeTimestamp }}"
          severity: warning
      - alert: RDOTrunkRepoTooOld
        annotations:
          summary: "No new activity in {{ $labels.worker }} for the last two hours"
        expr: "time() - dlrn_last_update{instance=~'centos9-.*'} > 7200"
        labels:
          lasttime: "{{ $value | humanizeTimestamp }}"
          severity: warning
