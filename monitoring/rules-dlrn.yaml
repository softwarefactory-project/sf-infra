# Code generated by dhall-to-yaml.  DO NOT EDIT.
groups:
  - name: dlrn.rules
    rules:
      - alert: RDOTrunkRepoTooOld
        annotations:
          summary: "No new activity in {{ $labels.worker }} for the last two hours"
        expr: "time() - dlrn_last_update{index=~'centos8-.*'} > 7200"
        labels:
          severity: warning
      - alert: RDOTrunkRepoTooOld
        annotations:
          summary: "No new activity in {{ $labels.worker }} for the last two hours"
        expr: "time() - dlrn_last_update{index=~'centos9-.*', index != 'centos9-master'} > 7200"
        labels:
          severity: warning
      - alert: RDOTrunkRepoTooOld
        annotations:
          summary: "No new activity in {{ $labels.worker }} for the last seven hours"
        expr: "time() - dlrn_last_update{index='centos9-master'} > 25200"
        labels:
          severity: warning
