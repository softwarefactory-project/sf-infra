# Code generated by dhall-to-yaml.  DO NOT EDIT.
groups:
  - name: blackbox.rules
    rules:
      - alert: WebDown
        expr: "probe_success{job='blackbox'} == 0"
        for: "10m"
        labels:
          severity: critical
      - alert: SSLCertExpiringSoon
        expr: "probe_ssl_earliest_cert_expiry{job='blackbox'} - time() < 86400 * 6"
        for: "1d"
        labels:
          severity: critical
      - alert: HighHTTP503Errors
        annotations:
          description: |
            The service {{ $labels.instance }} has been consistently returning HTTP 503 errors in the last 10 minutes.
          summary: "Unresponsive service (instance {{ $labels.instance }})"
        expr: "rate(http_requests_total{status='503'}[10m]) > 0"
        for: "10m"
        labels:
          severity: critical
