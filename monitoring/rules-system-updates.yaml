# Code generated by dhall-to-yaml.  DO NOT EDIT.
groups:
  - name: system-package-update-sf.rules
    rules:
      - alert: SystemOutdatedPackagesSoftwareFactory
        annotations:
          summary: "There are some packages to update in the SoftwareFactory infra!"
        expr: |
          (system_package_update{instance=~".*.softwarefactory-project.io:9100"}) > 0
        for: "7d"
        labels:
          severity: info
      - alert: SystemOutdatedPackagesRDO
        annotations:
          summary: "There are some packages to update in the RDO infra!"
        expr: |
          (system_package_update{instance=~".*.(openstack.org|rdoproject.org):(9100|9110|9111|9112|9113)"} and system_package_update{instance!~"(dashboards|trunk.+|www|centos.+|dlrn.*|rpm.*).rdoproject.org:9100"}) > 0
        for: "7d"
        labels:
          severity: info
