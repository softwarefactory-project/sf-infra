groups:
  - name: system-package-update-rdo.rules
    rules:
      - alert: SystemOutdatedPackagesRDO
        annotations:
          summary: "There are some packages to update in RDO infra!"
        expr: |
          system_package_update{instance=~"(centos.+|dlrn.+|rpm.+|trunk.+|www).rdoproject.org.+"} > 1
        for: "7d"
        labels:
          lasttime: "{{ $value | humanizeTimestamp }}"
          severity: info
      - alert: SystemOutdatedPackagesRDO
        annotations:
          summary: "There are many packages to update in RDO infra!"
        expr: |
          system_package_update{instance=~"(centos.+|dlrn.+|rpm.+|trunk.+|www).rdoproject.org.+"} > 100
        for: "14d"
        labels:
          lasttime: "{{ $value | humanizeTimestamp }}"
          severity: warning
