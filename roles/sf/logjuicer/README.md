Role to deploy Logjuicer
========================


# Variables:

* `host_port`:  The address on which to expose the web interface and. Defaults to: 30000.
* `container_port`:  The address on which to expose the web interface and. Defaults to: 3000.
