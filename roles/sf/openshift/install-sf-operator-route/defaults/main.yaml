---
kube_config: "{{ ansible_user_dir }}/.kube/config"
route_name: gateway
route_namespace: sf
route_host: sfop.me
route_target_port: 8080
route_target_service: gateway
route_labels: {}
route_enable_le: false
route_le_issuer_server: https://acme-v02.api.letsencrypt.org/directory
route_le_issuer_name: le-issuer-prod
route_le_issuer_namespace: sf
