- name: Remove udp-multiplexer directory
  become: true
  ansible.builtin.file:
    path: /opt/udp-multiplexer
    state: absent

- name: Remove service
  become: true
  ansible.builtin.systemd:
    name: udp-multiplexer
    state: stopped
    enabled: false
