---
- name: Prepare
  hosts: all
  tasks:
    - name: Install packages
      package:
        name:
          - openssh-server
          - python3-libselinux
          - python3-policycoreutils

    - name: Enable sshd
      systemd:
        name: sshd
        daemon_reload: yes
        enabled: yes
        state: started
