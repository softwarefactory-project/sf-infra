- name: Create cron job for pruning weak images
  cron:
    name: Prune weak images
    special_time: "{{ pruning_time }}"
    job: "podman image prune -f {{ extra_attributes }}"
    user: "{{ user }}"
  become: true
