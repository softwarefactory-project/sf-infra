---
- name: Converge
  hosts: all
  vars:
    dlrn_server_type: primary
    dlrn_enable_https: false
    dlrn_host: test
    client_cert: 'blabla'
    client_key: 'foobar'
    dlrn_db_password: 'password'
    puppet_dlrn_path: "{{ lookup('env','MOLECULE_PUPPET_DLRN_PATH') }}"
  roles:
    - role: dlrn-server
