---
- name: Rebuild CRC image
  hosts: image-builder.softwarefactory-project.io
  tasks:
    - name: Trigger script for creating CRC snapshot
      ansible.builtin.command: /usr/local/bin/recreate-crc-snapshot.sh
