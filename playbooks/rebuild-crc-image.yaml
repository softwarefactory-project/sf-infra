---
- name: Rebuild CRC image
  hosts: image-builder.softwarefactory-project.io
  tasks:
    - name: Trigger script for creating CRC
      ansible.builtin.command: /usr/local/bin/recreate-crc-images.sh
