networks: []
subnets: []
routers: []

security_groups:
  - name: common
    rules:
      - port: 22
      - port: -1
        protocol: icmp

  - name: zuul-console
    rules:
      - port: 19885

image_cache_dir: "{{ ansible_user_dir }}/image_cache"
images: []
