firewall_rules:
    - port: '9100/tcp'
      permanent: yes
      immediate: yes
      state: enabled
    - port: '80/tcp'
      permanent: yes
      immediate: yes
      state: enabled
    - port: '443/tcp'
      permanent: yes
      immediate: yes
      state: enabled
